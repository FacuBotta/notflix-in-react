import React, {useContext, useState} from "react";
import { searchsContext } from "../context/searchsContext";

function listeResults() {

    const {results} = useContext(searchsContext)

    return (

    )
}

export default listeResults;